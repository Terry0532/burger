{{#each burger}}
{{#unless devoured}}
<p>{{burger_name}}<button class="devoureIt" type="submit" burgerId="{{id}}">devoure it!</button></p>
{{/unless}}
{{/each}}

{{#each burger}}
{{#if devoured}}
<p><del>{{burger_name}}</del></p>
{{/if}}
{{/each}}

<form>
    <textarea name="burger" id="burgerName" cols="30" rows="10"></textarea>
    <button id="submit" type="submit">submit</button>
</form>